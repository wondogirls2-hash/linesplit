/** Canonical site URL for sitemap / OG — replace when domain is live */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://linesplit.app";

export const SITE_NAME = "LineSplit";

export const OG_IMAGE_PATH = "/og.png"; // placeholder until real asset is added
